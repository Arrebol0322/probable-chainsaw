<UserControl x:Class="ProductMonitor.MyUserControl.RingUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProductMonitor.MyUserControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="LayOutGrid">
        <!--  绘制圆  -->
        <Ellipse
        Width="{Binding ElementName=LayOutGrid, Path=ActualHeight}"
        Height="{Binding ElementName=LayOutGrid, Path=ActualWidth}"
        Stroke="#22ffffff"
        StrokeThickness="9" />
        <!--  按照百分比对圆涂色,并设置线条两端为圆弧状  -->
        <Path
        x:Name="path"
        Stroke="Orange"
        StrokeEndLineCap="Round"
        StrokeStartLineCap="Round"
        StrokeThickness="8" />
        <!--  百分比文本  -->
        <TextBlock
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Foreground="White">
        <Run FontSize="20" Text="{Binding PercentValue, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0:#.#}}" />
        <Run Text="%" />
        </TextBlock>
    </Grid>
</UserControl>
